export interface IAtividade {
    usuario: {
        id: string
    }
    atividadeTipo: '';
    descricaoAtividade: string;
    titulo: string;
    dataAtividade: string;
    imagemBase64: string;
    id: number
}
